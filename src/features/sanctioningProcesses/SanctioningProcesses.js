import { Box } from '@mui/material';

function SanctioningProcesses() {
  return <Box sx={{ width: '100%', minHeight: 'calc(100vh - 120px)', bgcolor: '#fff' }} />;
}

export default SanctioningProcesses;
